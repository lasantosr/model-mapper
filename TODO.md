# TODO

- review integration & insta tests
- the module-level comment of test must explain what is that file trying to cover (not specific tests, but behavior)
- keep integration tests them consistent: with mod, test after relevant struct, etc.
- review final tests names so that they're self-explanatory on the summary.
- add comments to tests so that they explain the intended features under test
- verify every feature of README has a test for it
- every attr or feature is covered on insta tests to verify the output
- verify all features intended for no_std are tested on the gate
- add domain unit tests

- generate cubic wiki after merge

- derive(ref = String) instead of ty, for the open issue?
